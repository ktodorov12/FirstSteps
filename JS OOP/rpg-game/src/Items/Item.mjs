export default class Item {
  constructor(type, name, stat, statType, bodyPart, rarity) {
    this.type = type;
    this.name = name;
    this.stat = stat;
    this.statType = statType;
    this.bodyPart = bodyPart;
    this.rarity = rarity;
  }
}